<!-- 本文件由 hack/gendoc 自动生成，请勿手改。改命令/标志请改 cmd/ 后重新 go run ./hack/gendoc -->

# 命令参考

按命令树排列。每条命令的用法、标志、子命令与 `somcli <命令> --help` 完全一致。

Structure-Projects Container Management CLI

### Synopsis

somcli is a unified management tool for container technologies including 
Docker, Docker Compose, Docker Swarm and Kubernetes.

### Options

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
  -h, --help                  help for somcli
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli apply](#somcli-apply)	 - Apply configuration
* [somcli cluster](#somcli-cluster)	 - Manage container clusters
* [somcli delete](#somcli-delete)	 - Delete resources
* [somcli describe](#somcli-describe)	 - Show details of a specific resource
* [somcli docker](#somcli-docker)	 - Manage Docker installation and operations
* [somcli docker-compose](#somcli-docker-compose)	 - Enhanced Docker Compose management
* [somcli download](#somcli-download)	 - Download offline resources
* [somcli get](#somcli-get)	 - Get resources
* [somcli images](#somcli-images)	 - Manage Docker images lifecycle
* [somcli install](#somcli-install)	 - Install system tools
* [somcli registry](#somcli-registry)	 - Manage container image registries
* [somcli status](#somcli-status)	 - Show what somcli has installed, and where
* [somcli uninstall](#somcli-uninstall)	 - Uninstall resources by running their remove_scripts
* [somcli validate](#somcli-validate)	 - Validate a config file without installing anything
* [somcli version](#somcli-version)	 - Print the version information

### somcli apply

Apply configuration

### Synopsis

Apply configuration to Kubernetes, Docker Swarm or Docker clusters.

```
somcli apply [flags]
```

### Options

```
  -h, --help   help for apply
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli cluster

Manage container clusters

### Synopsis

Create and manage container clusters including Kubernetes and Docker Swarm.

### Options

```
  -h, --help   help for cluster
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI
* [somcli cluster add-node](#somcli-cluster-add-node)	 - Add a node to an existing cluster
* [somcli cluster create](#somcli-cluster-create)	 - Create a new cluster
* [somcli cluster remove](#somcli-cluster-remove)	 - Remove an existing cluster
* [somcli cluster remove-node](#somcli-cluster-remove-node)	 - Remove a node from an existing cluster

### somcli delete

Delete resources

### Synopsis

Delete resources from Kubernetes, Docker Swarm or Docker clusters.

```
somcli delete [flags]
```

### Options

```
  -h, --help               help for delete
  -n, --namespace string   Namespace
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli describe

Show details of a specific resource

### Synopsis

Show detailed information about a specific resource in Kubernetes, Docker Swarm or Docker clusters.

```
somcli describe [flags]
```

### Options

```
  -h, --help               help for describe
  -n, --namespace string   Namespace
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli docker

Manage Docker installation and operations

### Synopsis

The docker command provides installation, uninstallation and passthrough operations for Docker.
By default (without subcommands), it will install Docker on local machine.

For Docker commands, just use 'somcli docker [command]' to pass through to Docker CLI.

```
somcli docker [flags]
```

### Options

```
  -h, --help      help for docker
      --offline   Offline mode (no downloads)
  -y, --yes       Automatic yes to prompts
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI
* [somcli docker install](#somcli-docker-install)	 - Install Docker engine
* [somcli docker status](#somcli-docker-status)	 - Check Docker installation status
* [somcli docker uninstall](#somcli-docker-uninstall)	 - Uninstall Docker engine

### somcli docker-compose

Enhanced Docker Compose management

### Synopsis

Enhanced Docker Compose wrapper with additional features:

* Auto-install if not present
* Smart command defaults (e.g. 'up' defaults to -d)
* Proxy support for installation
* Environment file support
* Native signal handling

Examples:
  somcli docker-compose up          # Auto -d
  somcli docker-compose ps          # Auto -a
  somcli docker-compose logs -f     # Passthrough with signals
  somcli docker-compose --env-file .env.prod up

```
somcli docker-compose [flags]
```

### Options

```
  -e, --env-file string   Specify an alternate environment file
  -h, --help              help for docker-compose
      --path string       Custom installation path
  -y, --yes               Automatic yes to prompts
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI
* [somcli docker-compose install](#somcli-docker-compose-install)	 - Install Docker Compose
* [somcli docker-compose uninstall](#somcli-docker-compose-uninstall)	 - Uninstall Docker Compose
* [somcli docker-compose version](#somcli-docker-compose-version)	 - Show Docker Compose version

### somcli download

Download offline resources

### Synopsis

Download all required resources based on configuration file

```
somcli download [flags]
```

### Options

```
  -f, --file string   Download configuration file (required)
  -h, --help          help for download
  -q, --quiet         Quiet mode
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli get

Get resources

### Synopsis

Get resources from Kubernetes, Docker Swarm or Docker clusters.

```
somcli get [flags]
```

### Options

```
  -A, --all-namespaces     All namespaces
  -h, --help               help for get
  -n, --namespace string   Namespace
  -o, --output string      Output format
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli images

Manage Docker images lifecycle

### Synopsis

The docker-images command provides full lifecycle management for Docker images including pull, push, export and import operations.

### Options

```
  -h, --help   help for images
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI
* [somcli images export](#somcli-images-export)	 - Export images to file
* [somcli images import](#somcli-images-import)	 - Import images from file
* [somcli images pull](#somcli-images-pull)	 - Pull images from registry
* [somcli images push](#somcli-images-push)	 - Push images to registry

### somcli install

Install system tools

### Synopsis

Install resources declared in a config file.

Each resource is processed as: download urls -> distribute to hosts -> render
extra_files -> run pre_install -> apply method -> run post_install.

The `method` field selects how the resource is actually installed:

  script     (default) the pre_install / post_install scripts are the install
  binary     unpack the artifact if needed, install executables into install_dir
  package    hand the package name to the target machine's package manager
  container  pull image and drop a wrapper script named after the resource
  source     unpack the source archive into {{.SrcDir}} and run build:
  manifest   pipe each urls: / extra_files: manifest to kubectl apply

A resource is skipped when its check: probe exits 0, or when state.json already
records it at the declared version. Bump version:, or pass --force, to re-apply.

`method: package` needs administrator rights on the target machine. Pass
--sudo to prefix the package manager command with sudo; it is off by default, so
without it the generated command is exactly what it has always been.
`on_error: continue` keeps the run going past a failed resource (the exit
code is still non-zero); the default aborts at the first failure.

```
somcli install [flags]
```

### Examples

```
  # Batch install from config
  somcli install -f configs/tools.yaml

  # Install a single resource declared in the config
  somcli install -f configs/tools.yaml -n jq
```

### Options

```
  -f, --file string    Installation config file path
      --force          Re-apply resources even if check: hits or state.json already records them
  -h, --help           help for install
  -n, --name string    Only install the resource with this name
      --parallel int   Run each script on up to N hosts concurrently (per-resource, hosts only) (default 1)
      --sudo           Prefix the package manager command with sudo (method: package only)
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli registry

Manage container image registries

### Synopsis

Commands for managing container image registries including Harbor installation and image synchronization

### Options

```
  -h, --help   help for registry
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI
* [somcli registry install](#somcli-registry-install)	 - Install Harbor registry
* [somcli registry sync](#somcli-registry-sync)	 - Sync images between registries
* [somcli registry uninstall](#somcli-registry-uninstall)	 - Uninstall Harbor registry

### somcli status

Show what somcli has installed, and where

### Synopsis

List the install records somcli keeps in <workdir>/state.json.

Each row is one resource on one target: a resource without hosts: is recorded
against "(local)", a resource with hosts: gets one row per host. This is the
same record install consults to decide whether a resource can be skipped.

```
somcli status [flags]
```

### Examples

```
  somcli status
  somcli status --workdir /opt/somwork
```

### Options

```
  -h, --help   help for status
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli uninstall

Uninstall resources by running their remove_scripts

### Synopsis

Run the remove_scripts of resources declared in a config file.

Resources are processed in reverse declaration order, so things are torn down
before whatever they depend on. A resource without remove_scripts is skipped
with a warning.

```
somcli uninstall [flags]
```

### Examples

```
  # Uninstall everything declared in the config
  somcli uninstall -f configs/tools.yaml

  # Uninstall a single resource
  somcli uninstall -f configs/tools.yaml -n jq
```

### Options

```
  -f, --file string   Config file path (required)
  -h, --help          help for uninstall
  -n, --name string   Only uninstall the resource with this name
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli validate

Validate a config file without installing anything

### Synopsis

Parse the config file and render every template it contains, then report.

Reads only: no download, no script execution, no file is written.
Exit code is non-zero if the config would fail at install time.

```
somcli validate [flags]
```

### Examples

```
  # Check a config before installing
  somcli validate -f configs/tools.yaml
```

### Options

```
  -f, --file string   Config file to validate (required)
  -h, --help          help for validate
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

### somcli version

Print the version information

### Synopsis

Print the version information for somcli command line tool.

```
somcli version [flags]
```

### Options

```
  -h, --help    help for version
  -j, --json    Print the version in JSON format
  -s, --short   Print just the version number
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli](#somcli)	 - Structure-Projects Container Management CLI

#### somcli cluster add-node

Add a node to an existing cluster

### Synopsis

Add a node to an existing cluster.

The node must already be listed under cluster[].nodes in the configuration file
(that is where its address, credentials and role live); --node names which one.

```
somcli cluster add-node [flags]
```

### Options

```
      --cluster-name string   Which cluster in the config to add to (see cluster[].name)
      --cluster-type string   Which cluster type in the config to add to (k8s|swarm)
  -f, --file string           Cluster configuration file (required)
      --force                 Re-apply resources even if check: hits or state.json already records them
  -h, --help                  help for add-node
      --node string           Hostname of the node to add, as written in cluster[].nodes (required)
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli cluster](#somcli-cluster)	 - Manage container clusters

#### somcli cluster create

Create a new cluster

### Synopsis

Create a new Kubernetes or Docker Swarm cluster based on configuration file.

```
somcli cluster create [flags]
```

### Options

```
      --cluster-name string   Which cluster in the config to create (see cluster[].name)
      --cluster-type string   Which cluster type in the config to create (k8s|swarm)
  -f, --file string           Cluster configuration file (required)
      --force                 Re-apply resources even if check: hits or state.json already records them
  -h, --help                  help for create
      --skip-precheck         Skip pre-installation checks
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli cluster](#somcli-cluster)	 - Manage container clusters

#### somcli cluster remove

Remove an existing cluster

### Synopsis

Remove an existing Kubernetes or Docker Swarm cluster based on configuration file.

```
somcli cluster remove [flags]
```

### Options

```
      --cluster-name string   Which cluster in the config to remove (see cluster[].name)
      --cluster-type string   Which cluster type in the config to remove (k8s|swarm)
  -f, --file string           Cluster configuration file (required)
      --force                 Force removal without confirmation
  -h, --help                  help for remove
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli cluster](#somcli-cluster)	 - Manage container clusters

#### somcli cluster remove-node

Remove a node from an existing cluster

### Synopsis

Remove a node from an existing cluster: drain it, reset it, then delete the Node object.

Use "cluster remove" to tear down the whole cluster; this command refuses to
remove the first master.

```
somcli cluster remove-node [flags]
```

### Options

```
      --cluster-name string   Which cluster in the config to remove from (see cluster[].name)
      --cluster-type string   Which cluster type in the config to remove from (k8s|swarm)
  -f, --file string           Cluster configuration file (required)
      --force                 Force removal without confirmation
  -h, --help                  help for remove-node
      --node string           Hostname of the node to remove, as written in cluster[].nodes (required)
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli cluster](#somcli-cluster)	 - Manage container clusters

#### somcli docker install

Install Docker engine

### Synopsis

Install Docker engine on local or remote nodes.
Examples:
  # Install latest Docker on local machine (default behavior)
  somcli docker
  
  # Install specific version on local machine
  somcli docker install --version 20.10.12
  
  # Install on remote nodes specified in file
  somcli docker install -f nodes.yaml
  
  # Install on specific remote nodes
  somcli docker install --node 192.168.1.101,192.168.1.102 --user root --ssh-key ~/.ssh/id_rsa

```
somcli docker install [flags]
```

### Options

```
      --data-path string      Custom data directory path
  -f, --file string           Path to YAML file containing node configurations
  -h, --help                  help for install
      --install-path string   Custom installation path
      --node strings          Comma-separated list of node IP addresses
      --ssh-key string        Path to SSH private key for remote nodes
      --user string           SSH user for remote nodes (default "root")
      --version string        Docker version to install (default "latest")
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               Offline mode (no downloads)
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
  -y, --yes                   Automatic yes to prompts
```

### SEE ALSO

* [somcli docker](#somcli-docker)	 - Manage Docker installation and operations

#### somcli docker status

Check Docker installation status

### Synopsis

Check Docker installation status on local or remote nodes.
Examples:
  # Check Docker status on local machine
  somcli docker status
  
  # Check status on remote nodes specified in file
  somcli docker status -f nodes.yaml
  
  # Check status on specific remote nodes
  somcli docker status --node 192.168.1.101,192.168.1.102 --user root --ssh-key ~/.ssh/id_rsa

```
somcli docker status [flags]
```

### Options

```
  -f, --file string      Path to YAML file containing node configurations
  -h, --help             help for status
      --node strings     Comma-separated list of node IP addresses
      --ssh-key string   Path to SSH private key for remote nodes
      --user string      SSH user for remote nodes (default "root")
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               Offline mode (no downloads)
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
  -y, --yes                   Automatic yes to prompts
```

### SEE ALSO

* [somcli docker](#somcli-docker)	 - Manage Docker installation and operations

#### somcli docker uninstall

Uninstall Docker engine

### Synopsis

Uninstall Docker engine from local or remote nodes.
Examples:
  # Uninstall Docker from local machine
  somcli docker uninstall
  
  # Uninstall from remote nodes specified in file
  somcli docker uninstall -f nodes.yaml
  
  # Uninstall from specific remote nodes
  somcli docker uninstall --node 192.168.1.101,192.168.1.102 --user root --ssh-key ~/.ssh/id_rsa

```
somcli docker uninstall [flags]
```

### Options

```
  -f, --file string      Path to YAML file containing node configurations
  -h, --help             help for uninstall
      --node strings     Comma-separated list of node IP addresses
      --ssh-key string   Path to SSH private key for remote nodes
      --user string      SSH user for remote nodes (default "root")
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               Offline mode (no downloads)
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
  -y, --yes                   Automatic yes to prompts
```

### SEE ALSO

* [somcli docker](#somcli-docker)	 - Manage Docker installation and operations

#### somcli docker-compose install

Install Docker Compose

```
somcli docker-compose install [version] [flags]
```

### Options

```
  -h, --help   help for install
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --path string           Custom installation path
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
  -y, --yes                   Automatic yes to prompts
```

### SEE ALSO

* [somcli docker-compose](#somcli-docker-compose)	 - Enhanced Docker Compose management

#### somcli docker-compose uninstall

Uninstall Docker Compose

```
somcli docker-compose uninstall [flags]
```

### Options

```
  -h, --help   help for uninstall
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --path string           Custom installation path
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
  -y, --yes                   Automatic yes to prompts
```

### SEE ALSO

* [somcli docker-compose](#somcli-docker-compose)	 - Enhanced Docker Compose management

#### somcli docker-compose version

Show Docker Compose version

```
somcli docker-compose version [flags]
```

### Options

```
  -h, --help   help for version
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --path string           Custom installation path
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
  -y, --yes                   Automatic yes to prompts
```

### SEE ALSO

* [somcli docker-compose](#somcli-docker-compose)	 - Enhanced Docker Compose management

#### somcli images export

Export images to file

```
somcli images export [flags]
```

### Options

```
  -f, --file string     Custom image list file
  -h, --help            help for export
  -o, --output string   Output archive file (default "images.tar.gz")
  -r, --repo string     Source registry repository
  -s, --scope string    Image scope (harbor|k8s|all) (default "all")
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli images](#somcli-images)	 - Manage Docker images lifecycle

#### somcli images import

Import images from file

```
somcli images import [flags]
```

### Options

```
  -h, --help           help for import
  -i, --input string   Input archive file (default "images.tar.gz")
  -r, --repo string    Target registry repository
  -s, --scope string   Image scope (harbor|k8s|all) (default "all")
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli images](#somcli-images)	 - Manage Docker images lifecycle

#### somcli images pull

Pull images from registry

```
somcli images pull [flags]
```

### Options

```
  -f, --file string     Custom image list file
  -h, --help            help for pull
  -o, --output string   Output file for pulled images list
  -r, --repo string     Target registry repository
  -s, --scope string    Image scope (harbor|k8s|all) (default "all")
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli images](#somcli-images)	 - Manage Docker images lifecycle

#### somcli images push

Push images to registry

```
somcli images push [flags]
```

### Options

```
  -h, --help           help for push
  -i, --input string   Input file with images list
  -r, --repo string    Target registry repository
  -s, --scope string   Image scope (harbor|k8s|all) (default "all")
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli images](#somcli-images)	 - Manage Docker images lifecycle

#### somcli registry install

Install Harbor registry

```
somcli registry install [flags]
```

### Options

```
      --ca-path string    Path to CA certificate files directory
  -h, --help              help for install
  -H, --hostname string   Harbor hostname (e.g. harbor.example.com) (required)
  -v, --version string    Harbor version to install (e.g. v2.5.0) (default "v2.5.0")
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli registry](#somcli-registry)	 - Manage container image registries

#### somcli registry sync

Sync images between registries

```
somcli registry sync [flags]
```

### Options

```
  -c, --concurrency int     Number of concurrent sync operations (1-10) (default 3)
  -h, --help                help for sync
  -f, --image-list string   Path to file containing list of images to sync (one per line) (required)
  -p, --password string     Registry password (or use REGISTRY_PASSWORD env)
  -s, --source string       Source registry URL (e.g. registry-1.docker.io) (required)
  -t, --target string       Target registry URL (e.g. https://harbor.example.com) (required)
  -u, --username string     Registry username
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli registry](#somcli-registry)	 - Manage container image registries

#### somcli registry uninstall

Uninstall Harbor registry

```
somcli registry uninstall [flags]
```

### Options

```
  -h, --help              help for uninstall
  -H, --hostname string   Harbor hostname that was installed (optional)
  -v, --version string    Harbor version that was installed (optional)
```

### Options inherited from parent commands

```
      --config string         config file (default is $HOME/.somcli.yaml)
      --debug                 enable debug mode
      --github-proxy string   GitHub proxy URL (e.g. https://gh-proxy.com/)
      --offline               enable 离线模式
      --set stringArray       Set a template variable, repeatable (e.g. --set port=8080). Overrides vars: in the config file
      --workdir string        working directory (default is ./somwork if exists, otherwise current directory)
```

### SEE ALSO

* [somcli registry](#somcli-registry)	 - Manage container image registries

