# runc —— containerd 真正用来起容器的那一层。
resources:
  - name: "runc"
    version: "1.1.14"
    urls:
      - "https://github.com/opencontainers/runc/releases/download/v{{.Version}}/runc.{{.Arch}}"
    target: "{{.Filename}}"
    post_install:
      - "install -m 755 {{.CacheDir}}/{{.Filename}} /usr/local/sbin/runc"
